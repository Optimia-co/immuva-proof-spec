# Schemas (v1) — NON NORMATIF

Ces schémas sont **strictement non-normatifs**.
Ils existent uniquement pour le **tooling** (validation, ergonomie, génération d’artefacts) et **NE DOIVENT PAS**
être utilisés comme référence de spécification.

La source normative unique est : `spec/v1/` + `registries/v1/` + `conformance/v1/` (selon V/W).

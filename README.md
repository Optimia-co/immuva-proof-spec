Reference implementation: `@immuva/sdk@0.2.0`  
Conformance: 20/20 tests passing

# immuva-proof-spec

Repo **normatif** pour **Immuva v1**.

- `spec/v1/` : texte A→Z (normatif)
- `registries/v1/` : registries W (normatifs si référencés)
- `conformance/v1/` : vectors V (normatifs : `expected/`)

`schemas/` et `golden/` sont **non-normatifs** (tooling / debug).
